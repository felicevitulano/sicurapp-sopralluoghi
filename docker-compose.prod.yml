version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: sicurapp_db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER} -d ${DB_NAME}']
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./packages/backend
      dockerfile: Dockerfile
    container_name: sicurapp_backend
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
      PORT: 3001
      MEDIA_DIR: /app/media
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      BASE_URL: http://187.124.0.120:8090
    volumes:
      - /opt/sicurapp-media:/app/media
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./packages/frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://187.124.0.120:8090
    container_name: sicurapp_frontend
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: sicurapp_nginx
    ports:
      - '8090:80'
    volumes:
      - ./nginx/nginx.prod.conf:/etc/nginx/nginx.conf:ro
      - /opt/sicurapp-media:/var/www/media:ro
    depends_on:
      - frontend
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
